import { ReactElement } from "react";
import { LoginForm } from "../components/LoginForm";

export const Login = (): ReactElement => {
 return (
  <LoginForm className="w-1/2" onClick={() => { }} />
 )
}